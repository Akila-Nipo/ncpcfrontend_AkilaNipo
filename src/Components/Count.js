import React from 'react';
import Birthday from "../CountComponent/Birthday";
import '../CountComponent/App.css';

function Count(props) {
    return (
        <Birthday day={66} month={0}/>
    );
}

export default Count;